// primitives
// 1 -number
let a = 111;
let aa = 111.333;
//Special numeric Symbols
let aaa = Infinity; // It happens when divide into 0
let abb = 7 / 0;
let aaaa = NaN; //it happened when to do any mathematics operations with number and not number except of +
let ab = 4 / 'hello';

// 2 - string
let b = 'Hello world'; // immutable, also happens when compare something
let bb = 3 > 4;
let bbb = "Hello world!";
let bbbb = `Hello world!`;
let bbbbb = `${b}`;

// 3 - BigInt
let c = 1224n;

// 4 - boolean
let d = true;
let e = false;

// 5 - undefined -  value is not assigned
let f = undefined;

//6 - null -  non-existing object, typeof shows it like Object type
let g = null;

// 7 - Symbol
let h = Symbol('s');

// Special type
// 8 - Object
let obj = {1: 'uno'};

